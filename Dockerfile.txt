# 使用官方 Node 镜像
FROM node:18

# 创建并进入工作目录
WORKDIR /app

# 复制 package 文件并安装依赖
COPY package*.json ./
RUN npm install --production

# 复制所有源代码
COPY . .

# 暴露端口（Express 默认是 3000）
EXPOSE 3000

# 启动命令
CMD ["node", "index.js"]
